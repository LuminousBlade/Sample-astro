---
import BaseLayout from "../layouts/BaseLayout.astro";
import Red_Panda from '../Red_Panda.jpg';
const pageTitle = "Penguin Me";

const identity = {
name: "Sarah",
      country: "Canada",
      occupation: "Technical Writer",
      hobbies: ["basketball", "football", "cricket"],
};

const skills = ["Astro", "HTML", "CSS", "Javascript", "PHP", "MySQL", "Python"];
const skillcolor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---
<style define:vars={{skillcolor, fontWeight, textCase}}>
h1 {
color: purple;
       font-size: 4rem;
}
.skill {
color: var(--skillcolor);
       font-weight: var(--fontWeight);
       text-transform: var(--textCase);
}
</style>

<BaseLayout />

<h1>{pageTitle}</h1>
<h2>... and my new Astro site!</h2>

<p> I am {identity.name}. I live in {identity.country}. I work here as a {identity.occupation}. 
{ identity.hobbies.length >= 2 && 
  <li> Two of my favourtie hobbies are {identity.hobbies[0]} and {identity.hobbies[1]} </li> 
}
<ul>
<p> My skills are:</p>
{skills.map((skill)=> <li class="skill">{skill}</li>)}
</ul>
<p>This site will update as I complete more of the tutorial, so keep checking back and see how my journey is going!</p>
<p> Here are few facts about me. </p>
<img src={Red_Panda.src} alt="It is a red panda photo">
<p> Just kidding this is an animal </p> 
